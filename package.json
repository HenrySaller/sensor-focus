{
  "engines": {
    "node": "5.9.1"
  },
  "dependencies": {
    "bower": "^1.7.9",
    "compression": "^1.6.2",
    "express": "4.13.3"
  },
  "devDependencies": {
    "autoprefixer": "^6.7.5",
    "concurrently": "^2.0.0",
    "grunt": "^1.0.1",
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-uglify": "^2.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-postcss": "^0.8.0",
    "grunt-sass": "^2.0.0"
  },
  "scripts": {
    "start": "concurrently \"npm run sass\" \"npm run js\" \"npm run watch\" \"node server/app.js\" ",
    "postinstall": "bower install",
    "sass": "grunt sass && grunt postcss",
    "js": "grunt concat && grunt uglify",
    "watch": "grunt watch"
  },
  "private": true
}
