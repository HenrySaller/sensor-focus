{
  "name": "sensor-focus",
  "version": "0.0.1",
  "license": "MIT",
  "description": "Description",
  "repository": {
    "type": "git",
    "url": "https://github.com/"
  },
  "scripts": {
    "start": "concurrently \"npm run compass\" \"npm run js\" \"npm run watch\" \"node server/app.js\" ",
    "postinstall": "bower install && npm run compass && npm run js",
    "compass": "grunt compass:base",
    "js": "grunt concat && grunt uglify",
    "watch": "grunt watch"
  },
  "engines": {
    "node": "5.9.1"
  },
  "dependencies": {
    "express": "4.13.3",
    "bower": "^1.7.9"
  },
  "devDependencies": {
    "concurrently": "^2.0.0",
    "grunt": "^1.0.1",
    "grunt-contrib-compass": "^1.1.1",
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-uglify": "^2.0.0",
    "grunt-contrib-watch": "^1.0.0"
  }
}
